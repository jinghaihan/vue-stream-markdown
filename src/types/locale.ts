export interface LocaleConfig {
  button: {
    zoomIn: string
    zoomOut: string
    resetZoom: string
    preview: string
    source: string
    collapse: string
    copy: string
    download: string
    minimize: string
    maximize: string
    flipY: string
    flipX: string
    rotateLeft: string
    rotateRight: string
    previous: string
    next: string
    back: string
  }
  error: {
    vanilla: string
    image: string
    mermaid: string
    katex: string
    harden: string
  }
}
