export interface LocaleConfig {
  button: {
    zoomIn: string
    zoomOut: string
    resetZoom: string
    preview: string
    source: string
    collapse: string
    copy: string
    download: string
    minimize: string
    maximize: string
  }
  error: {
    vanilla: string
    image: string
    mermaid: string
    katex: string
    harden: string
  }
}
